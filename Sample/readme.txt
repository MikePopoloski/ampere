This sample demonstrates the usage of the Ampere build tool.

Run the amp.exe program to start the build. If you have the TcpWatcher.exe program running at the time, you can see the output as it receives TCP notifications of asset builds.